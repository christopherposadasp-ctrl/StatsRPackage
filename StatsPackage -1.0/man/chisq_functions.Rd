% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chisq-functions.R
\name{chisq_functions}
\alias{chisq_functions}
\alias{chisq_gof_probs}
\alias{chisq_gof_dist}
\alias{chisq_table}
\alias{table_props}
\title{Chapter 14 chi-square and contingency-table functions}
\usage{
chisq_gof_probs(
  observed,
  p = NULL,
  labels = NULL,
  min_expected = 5,
  alpha = 0.05,
  digits = 4,
  quiet = FALSE
)

chisq_gof_dist(
  x = NULL,
  observed = NULL,
  dist = c("exp", "norm", "pois", "unif"),
  k = NULL,
  breaks = NULL,
  params = NULL,
  estimate = FALSE,
  params_estimated = FALSE,
  min_expected = 5,
  alpha = 0.05,
  digits = 4,
  quiet = FALSE
)

chisq_table(
  observed,
  type = c("independence", "homogeneity"),
  correct = FALSE,
  min_expected = 5,
  alpha = 0.05,
  digits = 4,
  quiet = FALSE
)

table_props(
  observed,
  margin = c("row", "col", "overall"),
  digits = 4,
  quiet = FALSE
)
}
\arguments{
\item{observed}{Observed counts. For \code{chisq_gof_probs()} and grouped
\code{chisq_gof_dist()} inputs, use a nonnegative integer vector. For
\code{chisq_table()} and \code{table_props()}, use a nonnegative two-way table or
matrix.}

\item{p}{Probabilities under the null for \code{chisq_gof_probs()}. If \code{NULL},
equal probabilities are assumed.}

\item{labels}{Optional category labels for one-way grouped inputs.}

\item{min_expected}{Minimum expected count threshold used for warnings and,
where implemented, class combining.}

\item{alpha}{Significance level in (0, 1).}

\item{digits}{Integer number of decimal places used only for printed output.}

\item{quiet}{Logical; if \code{TRUE}, suppress printed output.}

\item{x}{Raw sample data for \code{chisq_gof_dist()}.}

\item{dist}{Distribution for \code{chisq_gof_dist()}: \code{"pois"}, \code{"norm"},
\code{"exp"}, or \code{"unif"}.}

\item{k}{Number of classes used for raw-data GOF tests when \code{breaks} is not
supplied. For continuous GOF tests, \code{k} must be large enough to leave
positive degrees of freedom after any parameter-estimation adjustment.}

\item{breaks}{Class boundaries for grouped \code{chisq_gof_dist()} input.}

\item{params}{Named list of distribution parameters when parameters are not
estimated from raw data. For \code{dist = "unif"}, use \code{min}/\code{max}
(or \code{lower}/\code{upper}).}

\item{estimate}{Logical; if \code{TRUE}, estimate parameters from raw data where
applicable. For \code{dist = "unif"}, raw-data estimation uses \code{min(x)}
and \code{max(x)} and is not supported for grouped counts.}

\item{params_estimated}{Logical; for grouped GOF inputs, whether supplied
parameters should count against the degrees of freedom.}

\item{type}{For \code{chisq_table()}, whether the interpretation is
\code{"independence"} or \code{"homogeneity"}.}

\item{correct}{Logical; if \code{TRUE}, apply Yates correction in the 2 x 2
case for \code{chisq_table()}.}

\item{margin}{For \code{table_props()}, one of \code{"row"}, \code{"col"}, or \code{"overall"}.}
}
\value{
For the chi-square test functions, a classed \code{htest_result} object with
statistic, df, p-value, critical value, rejection region, decision, and
diagnostic outputs such as observed counts, expected counts, contributions,
and residuals. For \code{chisq_table()}, standardized residuals are also returned.

For \code{table_props()}, a classed \code{table_props_result} object is returned with
the original table and the requested proportions.
}
\description{
\code{chisq_gof_probs()} performs a one-way chi-square goodness-of-fit test to
specified category probabilities.
}
\details{
\code{chisq_gof_dist()} performs a chi-square goodness-of-fit test to a
distribution, supporting at least Poisson, normal, exponential, and
continuous uniform models.

\code{chisq_table()} performs a chi-square test for independence or homogeneity
in an r x c contingency table.

\code{table_props()} computes descriptive row, column, or overall proportions
for a contingency table.
}
\section{Functions}{
\itemize{
\item \code{chisq_gof_probs()}: One-way chi-square GOF test to specified probabilities.

\item \code{chisq_gof_dist()}: Chi-square GOF test to a distribution.

\item \code{chisq_table()}: Chi-square test for a contingency table.

\item \code{table_props()}: Descriptive proportions for a contingency table.

}}
