% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power-functions.R
\name{power_functions}
\alias{power_functions}
\alias{power_z_mu}
\alias{power_t_mu}
\alias{power_p_z}
\alias{power_var_chisq}
\alias{power_var_ratio_F}
\title{Power functions}
\usage{
power_z_mu(
  mu_a,
  mu0,
  sigma,
  n,
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater"),
  digits = 4,
  quiet = FALSE
)

power_t_mu(
  mu_a,
  mu0,
  sigma_true,
  n,
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater"),
  paired = FALSE,
  method = c("welch", "pooled"),
  digits = 4,
  quiet = FALSE
)

power_p_z(
  p_a,
  p0,
  n,
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater"),
  digits = 4,
  quiet = FALSE,
  pooled = NULL,
  continuity = FALSE
)

power_var_chisq(
  sigma_a,
  sigma0,
  n,
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater"),
  digits = 4,
  quiet = FALSE
)

power_var_ratio_F(
  sigma_a,
  ratio0 = 1,
  n,
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater"),
  digits = 4,
  quiet = FALSE
)
}
\arguments{
\item{mu_a}{True mean value or vector of true means under the alternative.}

\item{mu0}{Null-hypothesis mean value or mean-difference value.}

\item{sigma}{Known population standard deviation input.}

\item{n}{Integer sample size or vector of sample sizes.}

\item{alpha}{Significance level in (0, 1).}

\item{alternative}{Alternative hypothesis direction: \code{"two.sided"}, \code{"less"}, or \code{"greater"}.}

\item{digits}{Integer number of decimal places used only for printed output.}

\item{quiet}{Logical; if \code{TRUE}, suppress printed output.}

\item{sigma_true}{True population standard deviation input used in t-test power calculations.}

\item{paired}{Logical; for \code{power_t_mu()}, whether the supplied summaries are for paired differences.}

\item{method}{Method for the two-sample sigma-unknown t-test power calculation.}

\item{p_a}{True proportion value or vector of true proportions under the alternative.}

\item{p0}{Null-hypothesis proportion value or difference in proportions.}

\item{pooled}{Logical; for two-sample proportion power, whether to use pooled standard errors.}

\item{continuity}{Logical; whether to apply the continuity correction where supported.}

\item{sigma_a}{True standard deviation value or vector under the alternative.}

\item{sigma0}{Null-hypothesis standard deviation for the chi-square variance test.}

\item{ratio0}{Null-hypothesis variance ratio for the F test.}
}
\description{
Computes power and type II error for common tests for means, proportions,
and variances.
}
\details{
These functions return classed result objects with unrounded stored values
and optional printed summaries controlled by \code{digits} and \code{quiet}.
}
\section{Functions}{
\itemize{
\item \code{power_z_mu()}: Power for z tests of means with known sigma.

\item \code{power_t_mu()}: Power for t tests of means.

\item \code{power_p_z()}: Power for z tests of proportions.

\item \code{power_var_chisq()}: Power for one-sample chi-square tests of variance.

\item \code{power_var_ratio_F()}: Power for two-sample F tests of a variance ratio.

}}
